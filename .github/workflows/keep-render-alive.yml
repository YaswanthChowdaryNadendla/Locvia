name: Keep Locvia Backend Alive

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Locvia backend
        run: |
          curl -f https://locvia-backend.onrender.com/api/health